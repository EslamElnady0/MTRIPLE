import 'package:flutter/material.dart';

class RelaxationTab extends StatelessWidget {
  const RelaxationTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
